---
type: decision-log
---
# Decisions (append-only)

> One bullet per decision. Newest at the bottom. Never edit or delete past lines —
> if a decision is reversed, append a new line that says so and links the old one.
> Format:  - [YYYY-MM-DD · who] DECISION — why. (supersedes: <prev> if any)

- [2026-06-15 · mian] Use a git-shared Obsidian vault as the single source of truth for project memory — because two people + two agents need one brain, and markdown-in-git merges cleanly when entries are append-only.
- [2026-06-19 · mian + Claude Code] Installed the `cala` skill (`npx skills add cala-ai/cala-skill`) into `.agents/skills/cala` (symlinked to Claude Code) — to query structured, sourced facts about real-world entities (companies, people, funding, investors) instead of web-scraping; ~9pp more accurate and ~8× more token-efficient than web search for entity facts. NOT YET FUNCTIONAL: needs a Cala API key (console.cala.ai/api-keys) and the Cala MCP server (`https://api.cala.ai/mcp/`) added to `.mcp.json`; until then any call halts rather than falling back to web search.
- [2026-06-19 · mian + Claude Code] Built the NL-housing POC data pipeline for the Trace decision graph, living in `Trace_Core/data/` (NOT this vault; Trace_Core is a separate repo). Chain: Cala `knowledge/search` (REST, key used in-request only — never written to disk per FORBID:secrets) → `nl_housing_evidence.json` (101 evidence items, 11 factor themes incl. grid-congestion/netcongestie, every item source+url+quote) → `nl_housing_timeseries.json` (48 reversible temporal StateEvents / 19 metrics) → Stage-2 `canonical/` (101 canonical Claims w/ claim_kind+predicate_type+scope+direction+mapping_strength, 71 Sources, 67 recovered Entities, 5 contested hypotheses H1-H5). Why: gives Trace a typed, provenance-bearing evidence base. Modeling choice: causal_token hypotheses correctly route to `requires_token_cause_module`, causal_type to `structured_tier_required` — no hypothesis can reach `resolved` (no DIRECTLY_TESTS yet). Deferred to human-in-the-loop: conditional_scope, mapping_strength scope-match, necessity/PN gating, competing-DAG authoring, ReviewerActions.
- [2026-06-19 · Codex] Before publishing this vault to GitHub, keep local agent permission files and transient Cala responses out of git via .gitignore, and add SECURITY.md push checks — because .claude/settings.local.json can contain machine-local API credentials and must never be committed under FORBID: secrets.
- [2026-06-19 · Codex] Draft the personal market decision tool PRD as a desktop decision cockpit using the Netherlands housing market as the demo case — because the product needs to show facts/evidence, expert contribution, personal variables, market-state distributions, insights, and actionable advice in one reviewable interface concept.
- [2026-06-19 · Codex] Use the Trace core protocol files as the MVP deterministic quant engine layer: static/LLM/human inputs create evidence, candidates, support groups, and optional expert-authored structures; Trace computes distribution, coverage, credibility, claim resolution, and GapDiagnostics — because product advice must be auditable and repeatable, not generated as ungrounded narrative.
- [2026-06-19 · Codex] Mark all NL-housing Trace_Core data provenance derived from Cala with `source_origin: "cala"`, `source_provider: "cala"`, and `ui_highlight_tags: ["source:cala", "provenance:cala"]`; preserve multi-source evidence in canonical claims instead of collapsing to the first source; current regenerated counts are 179 evidence items, 165 canonical sources, 117 recovered entities, 48 temporal observations, and 37 asserted dependencies — because the UI needs reliable source highlighting and reviewer-visible provenance quality flags. (supersedes counts in the earlier 2026-06-19 NL-housing POC decision)
- [2026-06-19 · Claude Code] MVP tech stack = Vite + React + TypeScript frontend; FastAPI + uvicorn warm local Python server for the Trace engine; desktop shell DEFERRED (web-first demo, Electron optional wrapper later, Tauri once Rust is installed). Refines (does not reverse) Codex's `app/` scaffold: keep the React/TS frontend and the copied engine, but expose the engine via the PRD's HTTP API boundary (`/api/run-default-tier|structured-tier|bridge|personal-advice`) instead of Electron `spawn python run_demo.py` per IPC call — because the PRD already specifies an HTTP API with POST bodies (personal vars, expert notes), a warm server avoids per-call Python cold-start + Windows execution-policy/path fragility, and lets the cockpit demo in a browser today. Added `app/engine/server.py` (FastAPI, reuses Codex's run_demo functions) + `app/requirements.txt`; did NOT touch Codex's main.cjs/App.tsx/package.json. Run: uvicorn server:app --app-dir app/engine --port 8000.
- [2026-06-19 · Codex] Configure the desktop MVP stack as Electron + Vite + React + TypeScript with the Python Trace engine invoked through a JSON subprocess/IPC boundary — because the current machine has Node/Python but no Rust, and this keeps the deterministic engine reusable while preserving a later Tauri sidecar migration path.
- [2026-06-19 · Codex] Accept Claude Code's FastAPI refinement as the default MVP path: Vite/React/TS web-first UI calls a warm local FastAPI + uvicorn Trace engine API; Electron is demoted to optional wrapper instead of the default engine bridge — because this matches the PRD HTTP boundary, avoids Electron install weight and per-click Python cold starts, and keeps the demo runnable in browser today. (supersedes Codex's prior default Electron subprocess decision; preserves Electron/Tauri as later shells.)
- [2026-06-19 · Codex] Build the first UI as a dense web-first decision cockpit wireframe with left navigation, top state bar, five PRD surfaces, right inspector, and deterministic engine output embedded where available — because the hackathon demo needs to show the product shape and evidence-to-advice traceability before final visual polish.
- [2026-06-19 · mian + Claude Code] Personal Fit = user-input + deterministic math, not fabricated static numbers. Surfaces that have no engine output (esp. personal exposure) are redesigned as USER INPUTS feeding a transparent deterministic layer (`app/engine/personal_fit.py`): affordability buffer / rate exposure / opportunity-cost-of-waiting / fragility / flexibility all computed from the user's own budget·down-payment·monthly-ceiling·quoted-rate + standard annuity math + real market anchors (`fixtures/nl_housing/market_anchors.json`, each carrying evidence_id, seeded from the Cala-derived timeseries) + the engine's real candidate_support distribution (weights the waiting-cost range). KEY: mortgage-rate LEVEL is NOT in the market data (only ecb_deposit_rate 2.25%) → `quoted_mortgage_rate` is a user input, never a fabricated default. This is "real" data because it is the user's own numbers, satisfying the real-data-only rule for the personal layer. Still TODO: wire personal_fit into `/api/run-personal-advice` (currently personal_variables is echoed, not used) + replace App.tsx hardcoded ImpactMeter/scenario-compare with these computed values.
- [2026-06-19 · mian + Claude Code] Real-data-only rule: any number shown to the user MUST come from real data — Cala output / the `fixtures/nl_housing/*` canonical fixtures (run through the engine via `fixtures_loader.py`) / or a tool-fetched (WebSearch/WebFetch) source carrying a url — never `run_demo.py`'s hand-typed `build_default_case()`/`build_structured_case()` placeholders. Why: the placeholder run gave shortage_dominates 33% + structurally_established, but the real Cala fixtures give a user_constraint-led 3-way tie (31.7/28.2/26.5) + graph_not_settled — placeholder numbers misrepresent the product. See FORBID line in constraints.md.
- [2026-06-19 · Codex] Tighten provenance semantics: only data directly read from raw Cala responses may carry `source_origin: "cala"` / `source:cala`; analyst/algorithmic objects such as hypotheses and asserted dependencies must use `derived_from_source_origins: ["cala"]` / `derived-from:cala`; independently fetched web/tool sources must not be counted as Cala — because the UI highlight must distinguish direct Cala provenance from downstream derivation or separate web retrieval.
- [2026-06-20 · Codex] Store NL housing factor research as a durable L1-L4 Cala-backed research asset under work/nl_housing_factor_research/ (28 raw Cala responses + factor_tree.json + summary + coverage report), with no API key persisted — because future UI/engine work must consume traceable real-data nodes instead of chat-only analysis or placeholder evidence.
- [2026-06-19 · Claude Code] Populated brain/north-star.md from the PRD: project = desktop decision-intelligence cockpit (facts + expert + personal vars → deterministic Trace engine → auditable personalized advice), demo case NL housing; done = cockpit shell + canonical NL case + Decision Brief + Personal Fit (vars change advice) + every number traces to engine output; non-goals = live data, pro mortgage/tax accuracy, automated/token causal inference, calibrated forecast probabilities, mobile, multi-user. OPEN/BLOCKING: exact hackathon event name + demo deadline still TBD — needs human confirmation.
- [2026-06-19 · Claude Code] Wired Codex's `app/engine/personal_fit.py` into `/api/run-personal-advice` (fulfils Codex's TODO from the Personal Fit decision). server.py maps request `personal_variables` -> personal_fit.Profile, calls compute() with the engine's REAL candidate_support, and returns `personal_fit` + a deterministic `personal_posture` whose confidence is capped by the engine's claim_resolution. PRD §15 respected: personal vars never touch evidence/support, only the exposure + posture layer. Verified: lowering monthly_ceiling flips buffer negative -> posture flips "Buy selectively" -> "Wait or resize"; omitting quoted_mortgage_rate_pct returns needs_user_input (no fabrication). FORBID compliance: removed server.py's run_demo fallback — fixtures missing now RAISES instead of serving placeholder numbers.
- [2026-06-20 · mian + Claude Code] PRD V2 revision (appended as §22, supersedes the EMPHASIS of §6/§8/§15/§16; Codex's v1 text preserved). Recenter: the conditioned causal exhibit on a CANVAS is the hero, not the multi-axis Market Map. Core object = Curated Domain Model (one domain = one expert-maintained, live-fact-fed structured causal model). Core interaction = conditioning-not-collapsing (user privately injects variables, watches the shared structure re-weight FOR them; output is a decision exhibit, not an answer). UI = canvas (the causal graph) + Trace engine running in the BACKGROUND + product as call-out panels (variables/distribution/indicators/evidence-inspector/story) summoned on demand — replaces v1's 5 fixed surfaces; interactive app, not a display page. Three on-canvas reads: causal graph (topology by problem type — transmission/decomposition/convergence) + probability distribution + key indicators (= thresholds DERIVED from scenario-flips or structured-tier splitting tests, not hand-picked). Monitor/update loop = retention engine + slow calibration line (distribution stays "relative support" until calibrated). Evidence Board preserved as the inspector drill-down (auditability not lost). MVP: NL housing (decomposition) end-to-end on canvas, Cala pipeline as the "live facts" refresh, no calibration claims yet.
- [2026-06-20 · Codex] Promote the NL housing L1-L4 Cala factor research into an app-facing fixture/API/UI path: generate `app/fixtures/nl_housing/factor_research.json` from `work/nl_housing_factor_research/factor_tree.json`, expose `/api/factor-tree` and `/api/personal-profiles`, and replace the React cockpit's placeholder Market/Evidence/Personal numbers with Cala-backed factor metrics, fixture profiles, and deterministic `personal_fit` output — because the MVP must obey the real-data-only rule while making the “every factor and next-level driver” research directly visible in the product.
- [2026-06-20 · mian + Claude Code] UI direction for the cockpit: a CANVAS, not fixed tabs. The causal graph is the canvas (pan/zoom, dot-grid background with snap-to-grid, draggable factor cards); the Trace engine runs in the background; product = call-out panels (Condition/Variables · Distribution · Evidence-inspector). Factor = a CARD component with an explicit info hierarchy — surfaced: category accent + name + weight `w%` (Fraunces, the hero) + latest value & trend arrow + evidence count/credibility/settled-or-contested + a weight bar; nested (click→inspector): claims, sources, quotes, time series, per-scenario support. Aesthetic taken from `trace-nordstream-case_v3v4.jsx` evidence graph: paper palette, Instrument Sans / JetBrains Mono / Fraunces-italic numbers, curved bezier edges with NO arrowheads (thickness = support strength, color = category, contested = primary dashed + graph_not_settled tag), hover focuses one node + dims the rest. Factor weight is Pearl-conditioned: w(f) = Σ_c r(c|user) · s(f→c) — the factor's contribution to the SCENARIOS that matter to this user; it flows as the user conditions. (Prototypes are visualize widgets in chat; the real component will be a React canvas calling the :8000 engine for real weights.)
- [2026-06-20 · mian] Repo IP hygiene = option B: purge ALL of work/ (PRD, app-tech-stack, decision-brief-copy, nl_housing_factor_research) from the PUBLIC repo history + gitignore + force-push; the Trace core engine (trace_engine/structured/bridge.py) was already purged earlier. work/ stays LOCAL ONLY. CONSEQUENCE: work/ no longer syncs via git, so Codex on another machine won't see the PRD — Codex must be oriented via brain/ (this log) + reference/architecture.md, which stay in git. brain/decisions.md (with detailed product decisions) stays public under B by the user's choice (option C would have purged it too).
- [2026-06-20 · Codex] Finalize provenance labeling semantics for Trace_Core NL-housing data: Stage-1 evidence items, direct sources, quotes, canonical `source_provenance`, and timeseries `source_provenance` may carry direct `source:cala`; canonical claims, evidence scoring fields, hypotheses, timeseries observations, and asserted dependencies carry `derived-from:cala` only — because UI highlighting must point at true Cala-sourced material without overstating analyst-derived objects or separately fetched web/tool sources.
- [2026-06-20 · Codex] Replace the app-facing NL-housing factor fixture with a mixed-source L1-L4+ tree: 41 factors, 597 metric lines, 370 embedded source links, 337 direct Cala source links and 33 web/tool source links, with direct `source:cala` only on nested source objects extracted from raw Cala responses and factor nodes using `derived_from_source_origins`; added explicit supply/nitrogen/grid, borrow-interest, equity/wealth/liquidity, Box 3/rental-policy, demographics and macro breakdown nodes — because the UI needs to highlight true Cala material while still consuming broader web evidence for the current housing-price driver map.
- [2026-06-20 · Codex] Add a Dutch-language source sweep to the NL-housing mixed-source fixture: 12 Nederlandstalige web sources now carry `language: "nl"` and `source_origin: "web"` across CBS/Kadaster, CBS StatLine, Kadaster Vastgoeddashboard, Nibud hypotheeknormen 2026, NHG, Rijksoverheid Box 3, Wet betaalbare huur, Programma Woningbouw, BIJ12 stikstof, and Netbeheer Nederland capaciteitskaart; rollups are now 41 factors, 597 metric lines, 387 embedded source links, 337 direct Cala links, 50 web links and 28 web registry sources — because the initial web pass was English-heavy and Dutch official terminology is necessary for source completeness without mislabeling web evidence as Cala.
- [2026-06-20 · mian + Claude Code] CANONICAL factor + causal-edge spec for the NL-housing UI graph now lives at `reference/nl-housing-factor-tree.md` (git-synced so Codex sees it; pointer added to reference/architecture.md). It takes mian's 10-section tree, COMPLETES it (gap-scan added 9 logically-missing items: hypotheekrenteaftrek, transfer-tax/opkoopbescherming asymmetry, the explicit latent macro-regime CONFOUNDER node+edge, a reflexivity/expectations feedback loop, a forecast-dispersion node, bond/swap yields + rate-fixation curve, supply net-vs-gross + labor/material, LTV/interest-only/jubelton/rent-indexation/aging-headship, and personal-layer completeness for personal_fit), and adds the EDGES (the taxonomy alone was not a graph): financing/supply/investor/regional channels, the contested `mortgage_rate→house_price = graph_not_settled` edge with its `⟂ | borrowing_capacity` splitting test, and conditioning (non-causal) edges from the personal layer. Mapped to the engine's 5 candidate market-states. `app/fixtures/nl_housing/factor_research.json` (41 factors) should be reconciled TO this spec. An independent 06-20 Cala+web re-pass (using mian's key, x-api-key header / {"input":...} body / urllib not curl on Windows) confirmed national breadth is at the practical frontier and only refreshed real values (rate 3.6–4.3%, forecasts ING1.5/ABN3/Rabo3.1, overbidding cooled to 4.7% Q1'26, election held 29 Oct 2025) — see reference §E. Next frontier = city+profile DEPTH, not more breadth.
- [2026-06-20 · Codex] Add evidence-status semantics to all NL-housing research fixtures: each metric/source/evidence line is tagged as observed fact, policy in force, methodology, model estimate, policy target/proposal, forecast/expectation, risk scenario, analysis/news claim, rumor/unverified, or review-needed, with coarse `actuality_group` buckets for UI highlighting; ambiguous non-empty text falls back to `analysis_or_news_claim` rather than fact — because the product must separate already-real market data from policy/expectation/news effects without overstating uncertainty.
- [2026-06-20 · Codex] Replace the NL-housing evidence UI contract's five-layer actuality grouping with a single `probability_pct`: already happened / already-in-force / current methodology = 100%, model estimates start at 80%, analysis/news claims 60%, forecasts 55%, policy targets/proposals 50%, risk scenarios 35%, rumor/unverified 15%, with small source/framing adjustments — because the UI should show one intuitive likelihood number instead of making users reason through abstract status buckets.
- [2026-06-21 · Codex] Make component inspection a focus mode in the Trace canvas: clicking a factor/read/private node selects it, opens the right panel, zooms the camera into the node-centered neighborhood, and Esc exits focus back to the global graph fit — because the canvas should support local investigation without losing the whole causal map.
- [2026-06-20 · mian + Claude Code] Ported the chat decision-exhibit prototype into a REAL, iterable React/TS component: `app/src/TraceCanvas.tsx` (+ standalone view `app/canvas.html` + `app/src/canvas-main.tsx`, + handoff `app/src/TraceCanvas.README.md`). ADDITIVE ONLY — did NOT touch Codex's App.tsx / main.tsx / api.ts (Codex's `api.ts` already exposes `fetchFactorTree()`, `runDefaultTier/Bridge/PersonalAdvice`, `FactorResearch` types — the component consumes those). What it is (PRD §22 canvas): full-screen workspace (height defaults to 100%), pan (drag bg) + zoom (wheel/buttons) + dot-grid with snap + draggable factor CARDS + thin bezier edges that highlight & annotate (polarity ▲/▼ + support strength s) on hover. Each card surfaces: category accent (color=category only) · name · PRECISE Pearl weight `w%` (fixed 17px Fraunces, never size-scaled) + grey weight bar · latest value+trend · evidence count/credibility · ochre dot if contested. COLOR=ONE-MEANING (accent=category, grey=weight, ochre=contested, rust=price). Weight = w(f)=Σ_c r(c|user)·s(f→c) (`factorWeights()`); conditioning mirrors the engine (`conditionDistribution()`) for instant sliders. DATA: on mount `fetchFactorTree()`→`adaptFactorTree()`; falls back to inline `FALLBACK_FACTORS/EDGES` so it renders offline (chip shows live/offline). VIEW: `cd app && npm run dev` → http://127.0.0.1:5173/canvas.html (a vite is already running on 5173). SHIP INSIDE COCKPIT: `import TraceCanvas from "./TraceCanvas"; <TraceCanvas/>` in App.tsx's main canvas. EXTEND (grep `EXTEND` in the file + see README): (1) real per-factor weights from engine (replace conditionDistribution + empty `support:{}` in adaptFactorTree with runDefaultTier/runBridge + a real factor→candidate support map), (2) personal layer via runPersonalAdvice, (3) reverse-trace (hover scenario→light its factors), (4) evidence inspector wired to factor.sources, (5) timeline/turning-points, (6) topology generalization (transmission/convergence). All weight/layout logic is pure functions for easy swap+test. Verified: tsc --noEmit clean, vite transforms TraceCanvas.tsx to valid JS, canvas.html serves 200.
- [2026-06-20 · mian + Claude Code] Built the full decision-exhibit WORKSPACE shell from mian's wireframe (rough default, to iterate per-component). New files (additive; did NOT touch App.tsx/main.tsx/api.ts): `app/src/TraceWorkspace.tsx` (shell), `app/src/trace/CanvasGraph.tsx` (graph region, presentational; pan/zoom controlled by parent), `app/src/trace/model.ts` (shared data + pure algorithms: factorWeights / conditionDistribution / adaptFactorTree / INDICATORS / fallback dataset), `app/src/trace/UI_TASKS.md` (the component/task list). `canvas-main.tsx` now renders TraceWorkspace → view at /canvas.html (`TraceCanvas.tsx` kept as legacy single-graph). Layout per wireframe: Header (logo + Personal ver + topic title/subtitle + search-another-topic + 2 buttons) · LeftRail (legend + nav zoom) · CanvasGraph (left) · adjustable subtle Splitter (drag 32–82%) · right column of foldable sections (Add-variables CTA communal/private · Condition private inputs · Scenario distribution = "possibilities" · Node investigation = full data structure on select · Evidence) · BottomDock (indicators to watch). Data = fetchFactorTree + offline fallback; weights = Pearl `w(f)=Σ_c r(c|user)·s(f→c)` mirrored locally. NEXT = work the 12 items in UI_TASKS.md one at a time (mian gives per-component feedback). Verified: tsc --noEmit clean, vite serves all three files 200.
- [2026-06-20 · Codex] Make `TraceWorkspace` the default app entry while keeping the older `App.tsx` cockpit as reference/fallback — because the current product direction is canvas-first and the team should iterate from the same decision-exhibit surface at `/` and `/canvas.html`.
- [2026-06-20 · mian + Claude Code] Workspace UI iteration (UI_TASKS.md #2, #11): (a) NavRail (zoom controls) moved to canvas top-right, collapsed to a small ⤢ by default, expands on hover. (b) Legend moved to a horizontal bar spread along the canvas bottom. (c) Keyboard/pinch zoom now affects the CANVAS only, not the page — a global guard preventDefaults ⌘/Ctrl +/-/0 and Ctrl+wheel and redirects them to the canvas cam. (d) Bottom indicators dock redesigned: leading "Net Price Read" card = the dynamic STRONGEST HYPOTHESIS (top scenario name + live % from the conditioned distribution) + price-direction read + synthesis; then 6 indicator cards, each field stacked on its own line (no overlap), showing value · Δ delta+direction · price signal (↑ rust / ↓ navy) + note · watch trigger. Indicator values/deltas are real (nl_housing_timeseries.json, Cala-derived); priceSignal/trigger are reasoning for now (EXTEND: derive from structured-tier splitting tests + live engine). Files: app/src/TraceWorkspace.tsx, app/src/trace/model.ts (INDICATORS + NET_READ). Note: Codex also added CanvasFactor.weightReady → cards show "w —" + hide the bar when a factor has no engine support yet.
- [2026-06-20 · mian + Claude Code] Added a DEMOGRAPHY indicator to the bottom dock, verified via Cala/CBS: NL population still increasing but decelerating and entirely migration-driven — 2025 +87k → 18.13M, net migration +95k (307k in − 212k out), natural change NEGATIVE (−7.5k, deaths > births, so without migration the population would shrink); Q1 2026 only +~10k (lowest Q1 since 2015), immigration down to 66k. Indicator: now "+87k ’25", Δ↓ "Q1’26 +10k, low since ’15", priceSignal up (strength 0.5, weakening), note "growth all migration; inflow slowing", trigger "net mig → 0 (natural change already −)". Added to `app/src/trace/model.ts` INDICATORS. (Canvas Migration-demand factor still shows the older +103k ’24 value — could refresh for consistency.)
- [2026-06-20 · mian + Claude Code] Bottom-dock design polish (UI_TASKS #11): indicators now SORTED by priceStrength (strongest price influence first); the Net Price Read conclusion ("↑ Upward, decelerating") is now a FILLED LABEL (price-direction background color + paper text, positioned right after the NET PRICE READ tag) so the read is seen first, not buried in text. Each indicator keeps its top-right price ↑/↓ + 3-bar strength meter. Per mian: from now on update brain/decisions.md directly without asking.
- [2026-06-20 · mian + Claude Code] Workspace layout polish: (1) Splitter model changed from a canvas-fraction to a fixed right-column WIDTH (px) — default rightW=300, canvas is flex:1 so it takes ALL remaining space by default (max graph real estate on page load); splitter drag sets rightW (clamped 240–640). (2) Legend bar made subtle: removed its border/stroke and lowered the paper background to .45 opacity. Files: app/src/TraceWorkspace.tsx.
- [2026-06-20 · mian + Claude Code] Bottom dock compressed + restructured (UI_TASKS #11): killed the "Indicators to watch" header row; top = a CONCLUSION BAND — hero filled label "↑ Upward, decelerating" (price-direction bg) on the left with a small "Net price read" title above it, supporting info (strongest hypothesis + % + rationale) in a bordered column to the right; then a divider; then COMPRESSED indicator cells (label · price↑/↓+strength corner · value+Δdelta · source ref) with priceNote+trigger moved to hover tooltip. Added `source` to each Indicator in model.ts (CBS · DNB · Kadaster · ABF · banks). Footer states honestly: indicators are a STATIC snapshot from the Cala-derived timeseries, NOT live-tracked yet. Files: app/src/TraceWorkspace.tsx, app/src/trace/model.ts.
- [2026-06-20 · mian + Claude Code] Bottom dock: (1) added a FORCE-BALANCE bar under the indicators — a two-color stacked bar (rust = up pressure, navy = down pressure) where each segment's width = that indicator's priceStrength; header shows "↑ X% up vs ↓ Y% down" (current ~80/20 up). Hovering a bar segment or an indicator card highlights the matching one (shared hoverInd state, tie-back both ways). (2) Surfaced each indicator's TRIGGER on the card as a visible "⚑ flips …" line (the threshold at which the indicator turns), instead of only in the tooltip. Files: app/src/TraceWorkspace.tsx.
- [2026-06-20 · claude] Moved the Net Price Read conclusion OUT of the bottom dock and INTO the right column as the graph OUTPUT — "Output · all possibilities" section now leads with the filled net-read hero (PRICE_COLOR by direction) + synthesis note, above ScenarioBars (strongest hypothesis highlighted). Bottom dock is now purely "indicators to watch": indicator cards + a far-right dashed "＋ Add indicator to track" button (stub) so users can track their own variable. Merged the force-balance readout and the static-snapshot note context into a single header line (label + mini force bar + up/down %). Why: one conclusion, one place (right = output); bottom = watchlist, not a second verdict.
- [2026-06-20 · claude] Reworked conditioning into an Add-variable (private) FLOW instead of an always-on slider panel. New: PERSONAL_VARS catalog in model.ts (rate/ceil/hor recommended, flex optional; each carries a rationale + the factor ids it plugs into); a VariablePicker modal (recommended pre-checked) opened from the Personal CTA; on confirm the chosen vars render as private dashed nodes in a left gutter of the canvas, wired by edges into their target factors (personalLayer + CanvasGraph private layer), and the camera reveals the gutter. conditionDistribution now gates each variable by whether it is ACTIVE (inactive held at NEUTRAL), so adding a variable literally changes the output. A "Your variables" control panel replaces the old Condition fold. Added a top-right "Run protocol" button (Header) — outputs read the LAST-RUN snapshot (applied/appliedActive); editing marks dirty + "stale · run" until Run commits. Added NavRail full-screen toggle (Fullscreen API on the canvas region) and a panels on/off toggle (hides splitter + right column + bottom dock). Dropped the "Legend" word from LegendBar; added a "private" legend swatch. Why: variables should be explicitly plugged in by the user and the protocol re-run on demand, matching the cockpit mental model; the graph stays the single surface where conditioning is visible and the outcome node is the conclusion.
- [2026-06-20 · claude] BottomDock polish: (1) centered + enlarged the "static snapshot" provenance footer and replaced the word "Cala" with an inline SVG recreation of the cala wordmark (CalaMark; swap for the real PNG by dropping it at app/public/cala-logo.png and using <img>). (2) Moved the "+ Add indicator" control out of the indicators row and into the dock header (top-right, next to force balance) as a small dashed button, so the indicator cards reflow to fill the full width and get more room.
- [2026-06-20 · claude] Minimalist pass on the right column: (1) dropped the AddVariablesCTA helper sentence. (2) Moved the OUTPUT (net price read hero + scenario distribution) OUT of the right column and onto the CANVAS as a right-docked overlay (OutputPanel) — the conclusion now lives with the graph (outcome node + distribution), and the right panel is purely information-display (click a component → NodeInvestigation / private detail) with more reading space. (3) CalaMark now renders the real logo from app/public/cala_logo.png (user-provided, copied from repo-root cala_logo.png) instead of the hand-drawn SVG.
- [2026-06-20 · claude] Redesigned the bottom-dock indicators as distance-to-trigger GAUGES (per the live-monitor reference). Indicator model is now numeric: nowVal/lineVal/lo/hi/unit + a "moves" param tag (greek) replacing the old now/delta/trigger strings. Each card draws a horizontal track with a now-marker (open circle, colored by price direction), a trigger tick (dark vertical bar at the line), and an ochre gap segment between them; the hero readout is "<dist> to line" so you see how far each indicator is from flipping at a glance. ARMED/Breached badge from state. Force-balance header unchanged (still uses priceSignal/priceStrength). EXTEND: derive lineVal/lo/hi from the structured-tier splitting tests + live values.
- [2026-06-20 · claude] Navbar + framing redesign. (1) Reorganized the header (designer pass): brand | framed topic question + subtitle | right cluster [search · view-toggles(panels,fullscreen) · Run protocol]. Moved fullscreen + panels toggle OUT of the canvas NavRail INTO the header far-right (NavRail now only zoom/reset); search sits left of them, Run is the rightmost primary CTA. (2) Reframed the topic from "Should I buy a home…" to the more generic "Where are Dutch house prices headed?" set in Newsreader italic (added the font to canvas.html) to match the requested editorial style; subtitle frames it as all forces+weights then conditioned on the private read. (3) OutputPanel is now two-tier: public market read (NET_READ + scenario distribution = "the market · public") and a generated private decision ("your call · private") via new personalAdvice() in model.ts — deterministic synthesis from the conditioned distribution + active variables, shown only after variables are added and the protocol is run.
- [2026-06-20 · claude] Indicator + header refinements. (1) Header kept clean — removed the subtitle line; the context/status moved to a top-left on-canvas overlay (CanvasStatus: "Dutch housing market · forces & weights" + conditioned-causal-canvas · engine state). (2) Reframed the title to "What's moving the Dutch housing market?" (present/forward, market-level not just prices). (3) Indicator cards: dropped the invented "moves <greek>" tag and the ARMED badge; restored the quantified price-direction arrow (↑/↓, colored) + 3-bar strength meter; labels are now short plain names (no "vs … line"), not truncated; card made taller. Kept the distance-to-trigger gauge (now-marker, trigger-tick, "X to line"). (4) Source line: a small rounded serif "cala" badge when viaCala, otherwise just the src text (added viaCala flag; forecast=false as the external example). (5) Force balance: removed "· contested" and slimmed the bar (h9→5).
- [2026-06-20 · claude] Navbar + title-band restructure. (1) Brand block now shows the product hierarchy: "Trace" / "Personal" tag / "a decision-intelligence cockpit" tagline beneath (muted mono). (2) Moved the search bar next to the logo with a vertical divider between them; view-toggles + Run stay far-right (flex spacer between). (3) Moved the topic question OUT of the header INTO a full-width TopicBand stuck to the top of the canvas — filled background (paperDeep) strip, question centered (Newsreader italic), with "conditioned causal canvas" (left) and engine status (right) on the band. Canvas region is now a column [TopicBand | graph-area]; CanvasStatus overlay removed (folded into the band).
- [2026-06-20 · claude] Header/band fixes. (1) Brand block center-aligned so "Trace Personal" and the "a decision-intelligence cockpit" tagline stack centered. (2) TopicBand stripped to just the centered question (removed the left "conditioned causal canvas" label and the right engine-live chip). (3) Engine-live status moved to the header far-right, immediately right of the Run protocol button. (4) Fixed the graph overflowing the canvas: the SVG-in-flex-column needed minHeight:0 on the canvas column + minHeight:0/overflow:hidden on the graph-area wrapper (classic flexbox SVG blow-out).
- [2026-06-20 · claude] Added the recent change (delta) back to indicator cards: restored delta/deltaDir on the Indicator model and render a small muted "↑/↓ <amount>" token next to each indicator name (tooltip: change vs prior period). Magnitude + direction arrow, e.g. Price momentum "↓ 2.0pp", Building permits "↑ 1.5k".
- [2026-06-20 · claude] Header/band polish. (1) Right cluster reordered: engine-live status now sits LEFT of the Run protocol button (Run is the far-right element). (2) Panels-toggle icon changed from ▦ to ◨ (docked-panel, shows ▭ when hidden). (3) Brand block left-aligned as a block (Trace line + tagline share the left edge) while "Trace"/"Personal" are vertically centered to each other (row alignItems baseline→center). (4) TopicBand restyled: height 42→30 (narrower), higher-contrast fill (#E6DFCE), title now non-italic bold sans (Instrument Sans 700) — a formal, readable title bar instead of the decorative serif italic.
- [2026-06-20 · claude] Indicator row polish. (1) The change/delta token is now colored by direction (up→rust K.primary, down→navy K.secondary, flat→inkMute) + semibold for readability. (2) Indicator cards now flex 1 1 0 with minWidth 158 so they stretch to fit the dock viewport exactly (no scrollbar when the count fits). Hid the horizontal scrollbar (.trace-indrow ::-webkit-scrollbar display:none + scrollbar-width:none) and added pointer drag-to-scroll on the row for when there are too many indicators to fit.
- [2026-06-20 · claude] Layout: canvas now spans the FULL viewport width; side panels float on top. The body is a relative full-width container; the canvas fills it (absolute inset:0) so the TopicBand stretches edge-to-edge. The right column became a floating, closable overlay card (absolute top/right/bottom, boxShadow, ✕ to close + a ◧ reopen button top-right). Split panelsOn into rightOn/dockOn (independently closable; header ◨ is the master show/hide). BottomDock got an onClose ✕. OutputPanel shifts left (rightInset = rightW+26) when the right panel is open so they do not overlap, and back to the edge when it is closed. Removed the drag splitter. Reverted the brand Trace/Personal row to baseline alignment (block stays left-aligned). Also patched NodeInvestigation catName for the concurrently-expanded Category union (policy/regional/personal).
- [2026-06-20 · Codex] Redraw the NL-housing canvas graph as a branching factor tree instead of a flat first-N factor layout: root market-state -> major drivers -> selected subdrivers -> price/transaction outcome, with contains edges as the tree skeleton and separate causal/contested overlays into the outcome -- because the canvas needs to explain what drives the housing market before optimizing card-level visual polish.
- [2026-06-20 · Codex] Reorganize the NL-housing canvas graph into grouped mechanism zones -- Demand Side, Macro Economy, Supply Side, Policy Supply, Regional/Local, Personal Variables, and Outcome/Measurement -- while preserving parent-child contains edges inside each zone and causal/contested overlays into the outcome; this makes the graph readable as an economic factor map rather than a loose node tree.
- [2026-06-20 · Codex] Expand the grouped NL-housing canvas into a non-overlapping layered decomposition map: supply now unfolds from structural shortage -> housing permits/pipeline -> bottleneck breakdown -> nitrogen/grid/viability constraints -> municipal/land policy, using only fixture-backed factors and extra contains edges for visual hierarchy; this matches the intended product behavior of showing why each driver exists rather than just grouping labels.
- [2026-06-20 · Codex] Change the NL-housing graph layout to a wide, horizontally scrollable decomposition map: secondary factors point only to their immediate parent, only L2 drivers connect to the outcome, and all outcome/root/measurement conclusions sit in a right-side conclusion zone; node cards were enlarged so the default graph remains legible even when the canvas overflows.
- [2026-06-20 · Codex] Wire the canvas factor weights to the core protocol: Workspace now calls `/api/run-default-tier` for real fixture-backed candidate support, conditions that distribution with active private variables, and fills factor cards via `w(f)=sum_c r(c|user)*s(f->c)`. Until the engine exposes direct factor-to-candidate support, `s(f->c)` is derived from the curated factor tree topology and explicit driver/scenario mappings; if the engine support call fails, cards remain `w --` instead of showing fallback numbers.
- [2026-06-20 · Codex] Split the canvas workspace with a draggable vertical divider: the left side is now the fact/evidence/variables work area over the graph, while the right side is a dedicated Output/Read/Advice column with market read, possibility distribution, top weighted drivers, and personal/actionable advice. Grouping zones were simplified to soft background color fields with titles only, removing the prior left stroke and dashed component-like border treatment.
- [2026-06-21 · Codex] Finish the split-canvas layout against the current TraceWorkspace architecture: facts/evidence/private variables now live in a left work panel over the graph; the right panel is exclusively Output / Read / Advice and no longer turns into a factor inspector; a draggable vertical divider resizes that output panel; group zones remain soft color washes with titles, no left stroke or dashed component frame.
- [2026-06-20 · claude] Reverted the floating-panel layout back to DOCKED but closable (per user). TopicBand is now a full-width row between Header and body (spans the whole viewport). Body is a flex row: canvas (flex:1) + docked right column; closing the right column (✕) collapses it to a thin vertical "＋ Inputs" reopen tab and the canvas reflows to fill the freed width (blank canvas revealed, not a floating overlay). BottomDock likewise docked + ✕ close → thin "＋ Indicators to watch" reopen strip. OutputPanel always renders on the canvas (docked right column no longer overlaps it). Fullscreen now targets the WHOLE workspace (rootRef on the root div) instead of just the canvas, for presenting. Dropped "Not live-tracked yet." from the provenance footer. Moved the cala badge to the RIGHT of "src · <source>".
- [2026-06-20 · claude] Navbar: folded the topic INTO the header as an input-style field and killed the separate TopicBand row (canvas now joins the header directly — cleaner). The topic field sits centered (flex:1), filled (#E6DFCE), bold sans, with a search icon on its right that toggles an editable input (search / re-enter topic; stub since only one domain). Added a divider to the LEFT of the engine-live cluster, symmetric to the logo divider. Removed the old search-next-to-logo input and the unused TopicBand component.
- [2026-06-20 · claude] Topic field: set to Newsreader italic (editorial). Added a focus/expand interaction — clicking the field (or the search icon) stretches it to fill the center (symmetric ~gap to both dividers via the header gap), switches to focused styling (paper fill + navy 1.5px stroke + focus ring), and drops a suggestions panel of other contested topics (Nord Stream case study, Dutch housing, ECB rates, nuclear, lab-leak) each with a right-justified category label. mousedown-preventDefault on the icon + suggestion rows so input blur does not pre-empt clicks; Enter/Esc/outside-click closes. TOPIC_SUGGESTIONS const drives the list (stub select for now — single live domain).
- [2026-06-20 · claude] Topic field now has 3 smooth variants. REST: compact (max-width 720), warm chip (#E6DFCE), 1px rule border, calm muted icon (0.7 opacity). HOVER: stretches edge-to-edge (max-width via .28s cubic-bezier), bg lightens to #EFE9DB, border darkens to K.meta, soft lift shadow, icon brightens to inkSoft @ full opacity — an invitation, no dropdown. FOCUSED (click): same width (no re-animation hover→focus), paper fill, navy border + focus ring, navy icon, editable input + suggestions dropdown. expanded = hovered||editing drives width so the stretch only animates rest↔hover; hover→focus only swaps the "skin". Transitions tuned per-property so it reads natural.
- [2026-06-20 · claude] Added the Contributors mechanism (people whose added public source sustained un-challenged into the graph). Surfaced in the topic-field focus dropdown: a "Contributors · this graph" section listing each contributor with avatar (initials), name + verified ✓ mark, field/expertise, contribution credibility ( hip, green when ≥85
- [2026-06-20 · claude] Added the Contributors mechanism (people whose added public source sustained un-challenged into the graph). Surfaced in the topic-field focus dropdown: a 'Contributors · this graph' section — avatar (initials), name + verified check mark, field/expertise, contribution credibility (pct chip, green when >=85), and a DM action. Click contributor -> profile fake door; DM -> message fake door (alert coming soon). 'Other contested topics' suggestions now show a contributor avatar-stack + count too. Data: CONTRIBUTORS const + TOPIC_SUGGESTIONS.people; Avatar/VerifiedMark components; fakeDoor() helper. DM is a span (not button) to avoid nested-button HTML.
- [2026-06-20 · claude] Topic field default weight bumped to Newsreader italic 500 (display + input). Canvas: removed the bottom horizontal scrollbar — Codex had set the graph-area overflow to 'auto hidden' and the SVG to a fixed VBW px width + minWidth; reverted the SVG to width/height 100% so it fills the container and panning is via the camera drag (grab cursor by default), and set graph-area overflow to hidden. No native scroll.
- [2026-06-20 · claude] Fixed Codex's swapped layout. Codex had put Inputs&inspector INTO the canvas (as an undefined WorkPanel, crashing) and the Output in the docked right column. Reverted per user: OUTCOME (OutputPanel) is now a floating overlay that is PART OF THE CANVAS; the right column (old place) holds Inputs & inspector again (un-hid the content, relabeled header, dropped OutputColumn); the draggable divider divides canvas vs the inspector. Also removed the WorkPanel reference (the tsc/runtime crash) and set canvas overflow back to hidden (no scrollbar; SVG already width:100%).
- [2026-06-20 · claude] Layout per user's precise spec. Canvas is now FULL viewport width (absolute inset:0). The outcome (OutputPanel) is PART OF the canvas as a right-side region with a left border = the internal divider that splits canvas into left(graph)|right(outcome); no card chrome (paper bg, no shadow/radius). Inputs&inspector is an absolute overlay stuck to the right of the viewport (same width), boxShadow (no left divider — it just overlaps the canvas), closable (✕ → ◧ reopen at top-right). Removed the old flex divider between panel and canvas. When the inspector is open it overlaps the outcome region; close it to read the conclusion on the canvas.
- [2026-06-20 · claude] Right region polish per user. Removed the inspector's drop shadow — it now uses the same K.paper fill as the rest, no floating-card look (only the full-width canvas sits underneath). The graph|outcome boundary is now a DRAGGABLE divider (a 1px line with a 9px hit area at right:rightW, zIndex 9 so it's grabbable even over the inspector; hover darkens; reuses the splitDrag handler → setRightW from r.right - clientX, clamp 300-620). Dropped OutputPanel's borderLeft (the divider draws the line). Left = graph (canvas), right = outcome/inspector with ~12px padding = the spacing; drag to focus facts vs outcome.
- [2026-06-20 · claude] Decoupled the divider from the inspector. The draggable divider now adjusts ONLY the canvas split (graph | outcome region width = rightW); the Inputs&inspector panel is a FIXED 360px width (unchanged by dragging). Divider zIndex lowered to 4 (canvas-level, below the inspector overlay at 8) so it's a canvas control, not a panel resizer.
- [2026-06-20 · claude] Reworked the right side into three regions per the user's annotated design: graph | [draggable canvas divider] | outcome | [fixed line] | inspector. The canvas divider (zIndex 9, visible 1px line, hover-darken) sizes ONLY the outcome region — drag left = more outcome space, drag right = more input space (splitDrag subtracts the fixed 360 inspector offset; re-binds on rightOn). The inspector is a FIXED 360px panel with a fixed borderLeft line, closable. OutputPanel got a rightOffset prop so it sits between the divider and the inspector (offset 360 when open, 0 when closed). Header 'Inputs & inspector' -> 'Inputs'; the node-investigation FoldSection renamed to 'Inspector' (inspective panels under the title).
- [2026-06-20 · claude] Redesigned the outcome region as a product/UX pass. (1) Dotted background (radial-gradient 26px grid matching the canvas tw-dots) so the right region reads as one continuous canvas. (2) Content now drawn in the same card language as the graph factors via a new OutcomeCard (accent rail + mono label + serif figure + bold name + note + weight bar), grouped by hierarchy: L0 'The read · public' = net price read hero (apex rust card), L1 'All possibilities · ranked' = the 5 scenarios as ranked hypothesis cards (category-colored accent, leading one highlighted rust, weight % + bar + one-line meaning via SCEN_META), L2 'Your call · private' = the personal decision card (green private accent, editorial-italic headline). Replaced the old inline read/ScenarioBars layout.
- [2026-06-20 · claude] Redesigned the "Add private variables" picker (TraceWorkspace VariablePicker) as a UX pass: dropped the text-heavy checkbox+rationale list for a direct-manipulation panel — each row is a unit-typed icon (€/%/clock/region-pin via UnitIcon SVGs) + label + one-line hint + a 5-dot ImpactMeter + inline range slider + typeable NumberField (in-field € prefix / % · yr suffix, clamps to min/max/step). Touching any control auto-plugs that variable into the graph (setVal → include). Toggle var renders as a switch. Rationale moved to a hover title (de-cluttered).
- [2026-06-20 · claude] Reconsidered which personal variables actually move the read and EXTENDED the engine model (trace/model.ts): added two high-leverage levers — `price` (target home price, impact .95) and `deposit` (savings/down-payment, impact .6). conditionDistribution now computes loan = max(50k, price − deposit) instead of the hardcoded 355000; NEUTRAL price 395k − deposit 40k = 355k so the baseline read is byte-identical to before. PersonalVarDef gained `unit`, `impact`, `hint`. PERSONAL_VARS now ordered by impact; recommended (pre-checked) set changed from {rate,ceil,hor} to {price,ceil,rate} (price/ceiling/rate are the genuine top levers; horizon demoted to optional). Why: the target price + deposit set the loan, the dominant driver of the monthly payment — previously invisible/hardcoded, so the tool couldn't respond to the single biggest decision input.
- [2026-06-20 · claude] Corrected a core model misunderstanding: input and outcome are ONE canvas, not a graph + a separate outcome panel. Removed the standalone OutputPanel (it was the wrong model). The divider is now a focus LOCATOR — dragging it PANS the single canvas horizontally (cam.x): drag left reveals more outcome, drag right reveals more input; nothing reflows (components keep their canvas positions). Removed the rightW panel-resize state. The real outcome conclusions are the canvas's outcome-group nodes, already connected to the factors by edges (the derivation). NEXT (needs CanvasGraph, Codex-owned): render the protocol's probabilistic read (scenario distribution from dist/engine) + the personal call as on-canvas conclusion nodes wired to their supporting factors — grounded in the core-protocol output, not fabricated. OutputPanel/OutcomeCard/SCEN_META left as dead code for reuse.
- [2026-06-20 · Claude Code] REGEN COLLISION: a parallel regeneration of `app/fixtures/nl_housing/factor_research.json` reverted it from my 47-node/75-edge augmentation back to 41/40, silently dropping the 6 analyst gap-scan nodes (personal_affordability, macro_regime_confounder, expectations_reflexivity, forecast_dispersion, bond_swap_yields, mortgage_interest_deduction) + the 35 annotated causal/confounder/feedback/conditioning edges — while `reference/nl-housing-factor-tree.md` still declared 47/75, leaving spec and data inconsistent. Re-applied the augmentation (idempotent script kept local at `.cala_tmp/update_factors.py`) so the committed file is 47/75 again. DURABLE FIX NEEDED (Codex-owned): the factor_research.json generator (`app/build_fixtures.py` / the work/nl_housing_factor_research pipeline) must treat the 6 analyst nodes + non-containment edges as ADDITIVE — read them from the canonical spec `reference/nl-housing-factor-tree.md` (sections A/B/G) on every regen — or it will keep wiping them. The spec is the source of truth; the fixture is a render of it.
- [2026-06-20 · claude] Restored NavRail to the canvas TOP-LEFT (it was hidden under the right inspector at right:0/360); zIndex 6. Did option 1: inject the protocol READ as on-canvas conclusion cards. The scenario distribution from dist (engine output) becomes 5 ranked 'read_<scen>' CanvasFactor nodes at x=2300 (right of the outcome group), each category-colored, showing the scenario name + its probability % (weights merged) + supporting-factor count + bar; the per-scenario meaning goes in mechanism (inspector). Edges wire the outcome node -> each read node (relation causal). Passed allFactors/allEdges/allWeights to CanvasGraph. Grounded in the core-protocol distribution, nothing fabricated. Reveal them by dragging the focus divider left (pan right) or fit.
- [2026-06-20 · claude] Divider reworked into a CENTER FOCUS ANCHOR: hidden by default, fades in when the cursor nears the horizontal middle (canvas onMouseMove, |x-center|<70), non-blocking (anchor container pointerEvents:none; grip pointerEvents:auto only when shown). Dragging the grip pushes the WHOLE graph (pans cam.x) — left reveals more outcome, right investigates the causes; up-handler clears the anchor. Also wired the conclusion read nodes into the flowchart more fully: each read_<scen> now connects from BOTH the outcome node AND its top supporting factor (relation causal), so the left factor graph and the right reads read as one connected flowchart, not a single short edge.
- [2026-06-20 · mian + Claude Code] INTEGRATE (not either-or) the Claude/Codex factor-graph work via a durable ADDITIVE OVERLAY, superseding the manual re-apply from the regen-collision entry. New committed data file `app/fixtures/nl_housing/factor_overlay.json` (6 analyst gap-scan nodes + 29 annotated causal/confounder/feedback/conditioning edges, canonical def reference/nl-housing-factor-tree.md). `scripts/build_nl_housing_app_research_fixture.py` now merges it on every regen: overlay nodes appended if id absent, containment edges auto-built (tagged relation:contains) from all parents, overlay edges appended; rollups gain edge_count/edge_relation_counts/overlay_node_count + an `augmentation` block. Result is the UNION produced from scratch (no hand-editing): 47 factors / 75 edges (46 contains · 21 causal · 2 confounder · 2 feedback · 4 conditioning), metric lines 597→609, web source links 50→55 — so a Codex regen can no longer wipe the analyst layer. UI now DISPLAYS the enrichment: `app/src/trace/model.ts` adaptFactorTree stopped flattening every edge to contains — it splits on `relation`, draws containment as child→parent and the annotated edges as source→target with real sign (+/−/?) and relation; `app/src/api.ts` FactorResearch.edges type extended with relation/sign/strength/mechanism/contested; `app/src/trace/CanvasGraph.tsx` styles confounder (dashed meta), conditioning (green dotted), feedback (dashed navy) distinctly; contested stays dashed-warn. tsc --noEmit passes. Also reordered the right panel (TraceWorkspace): Inspector on top with bounded vertical scroll, Inputs (add-variable + your-variables) pinned to the bottom.
- [2026-06-20 · claude] QUEUED (do AFTER the Codex engine-data integration settles, per mian): (a) highlight the LEADING read card in rust on the canvas — needs CanvasGraph (option 2: per-node emphasis when it's the top hypothesis); (b) inject the personal decision (advice) as a 'Your call' personal-category node wired to the user_constraint read. Holding both until engine scenario ids / outcome-node / support fields are confirmed stable against the real runDefaultTier output, so the injected read_<scen> nodes bind to real data, not assumptions.
- [2026-06-20 · claude] Done (Codex offline, file free). Verified data integration: dist.w is always keyed by the 5 SCENARIOS ids (engineDist only seeds the prior), f.support is keyed by scenario id, outcome node found via role/category — so the injected read nodes bind to real engine output. (a) Leading-read rust emphasis: added a 'lead' prop to CanvasGraph; the node whose id === lead (read_<top-scenario>) renders with rust accent rail + rust 2.6px stroke + rust tint fill + rust %. (b) Personal decision node: inject a 'your_call' personal-category node (green) with advice.headline (body in mechanism→inspector), wired from read_user_constraint via a conditioning (green-dashed) edge; suppressed the 'w —' placeholder for role:outcome nodes without a weight so the call card stays clean.
- [2026-06-20 · claude] Made the injected read/call nodes inspectable with grounded detail (they weren't in factors, so selFactor missed them → inspector was blank). Added selRead (from ranked) + selCall (advice). ReadDetail: scenario name + probability % + meaning, then 'Driven by (N)' = the real factors with f.support[scen]>0 ranked by support strength s, plus 'Watch to flip' = mapped INDICATORS (now→line) via new SCEN_IND. CallDetail: the advice headline (editorial italic) + body + 'Conditioned on' the active private variables. Inspector header now shows Read·<scen> / Your call accordingly. All sourced from dist / f.support / INDICATORS / advice — nothing fabricated.
- [2026-06-20 · claude] (1) Indicator card: moved the delta token (e.g. '↓ 2.0pp') out of the name group and grouped it visually with the price signal on the right (delta + ↑/↓ arrow + strength bars together), per mian. (2) Added the read DIRECTION to the canvas read cards: SCEN_META gained a dir (up/down/flat) per scenario; the injected read node's trend is set from it; CanvasGraph renders a ↑(rust)/↓(navy) arrow left of the % for role:outcome conclusion nodes (skips flat). Shortage/financing=up, investor=down, regional/user-constraint=flat.
- [2026-06-20 · claude] Topic-dropdown 'Current understanding' block polish (4 pts): (1) gave the whole block a paperDeep background card (margin+padding+radius) so it stands out from the suggestions list; (2) moved the direction chip (↑ RISING) to its own line ABOVE the conclusion, and bolded the title (editorial italic 600, 16.5px); (3) enlarged the insight lines (10.5→13.5, ~ same as the topic rows) and split each into description (left) + its own data/direction chip (↑rust/↓navy + value) justified to the far right — GRAPH_READ.insights restructured to {text,val,dir}; (4) 'N people'→'contributors' and shrank the contributor avatars (22→17, ring matched to the card bg).
- [2026-06-21 · claude] Restructured the workspace: outcome CONCLUSIONS moved off the canvas into the right panel. Removed the on-canvas read_/your_call nodes (conclNodes/callNodes/edges); the 5 ranked scenario reads + net read + private call now render as the default (no-selection) state of the right inspector body via a new clickable OutcomeConclusions component (clicking a card → selRead/selCall detail; inputs stay pinned at the bottom). Panel default title 'Inspector' → 'Conclusions'. The canvas is now PURE causal reasoning; its terminal outcome node (price_transaction_outcome) is highlighted as `lead`.
- [2026-06-21 · claude] Re-laid-out the causal graph on the dot grid (graphic-designer rules, invented): columns flow cause→effect L→R indexed by causal DEPTH so all 6 drivers align in one column (COL.DRV=1144) that fans into market_state→price; column pitch 11·GRID (286), sibling row pitch 5·GRID (130) — both multiples of GRID(26) so spacing reads uniform. Groups are horizontal lanes stacked top→bottom with a uniform 24px gap; subfactors right-align toward their driver. Rewrote model.ts `layout` (added COL constant) + moved personalLayer private gutter to the bottom personal lane (PV_X 390→600, y→2160+) so it no longer collides with the now-taller supply lane. Updated CanvasGraph GROUPS lane-band rects to wrap the new positions + extended the dot-grid rect; outcome band retitled 'OUTCOME / PRICE'. Default camera k=0.46 + fitView on first factor load (graph is ~1914×2416).
- [2026-06-21 · claude] Removed the center focus divider (the ⇆ drag-anchor that panned the canvas). Deleted divHover state, splitDrag ref, the pointer-drag useEffect, and the canvas mouse-move/leave handlers — it added no UX value (pan/zoom already cover navigation).

- [2026-06-21 · claude] Production deploy = single VPS, same-origin: Caddy serves Vite `dist/` + reverse-proxies `/api/*` to uvicorn (127.0.0.1:8000), auto-TLS for megathon.traceintelligence.io. Artifacts in `deploy/`. Frontend built with `VITE_TRACE_API_URL=/api` to avoid CORS. Proprietary engine .py (gitignored) + prebuilt fixtures ship via rsync, NOT git; build_fixtures.py never runs on VPS (reads Windows-only canonical path). — simplest demo path, one box/one cert/no CORS.

- [2026-06-21 · Claude] Hardened `server.py` CORS — replaced `allow_origins=["*"]` with an explicit list (prod domain + localhost:5173 dev), overridable via `TRACE_ALLOWED_ORIGINS` env. Same-origin Caddy deploy means CORS only matters for local cross-port dev. Verified engine still imports + serves /api/health, /api/run-bridge, /api/factor-tree from fixtures in the VPS engine/+fixtures sibling layout.
 causal graph structure, node data, weights, and inspector-visible details without moving the shell.
- [2026-06-21 · Codex] Prototyped a single graph factor-card information hierarchy on `structural_shortage`/fallback `housing_shortage` only. The card keeps `w` as the primary scan metric (`market weight`), adds the top supported read (`drives <scenario> · s <support>`), keeps the observed metric/trend, evidence count/status, and parent roll-up. Why: users need to know both "how important is this node now?" and "what market read does it explain?" without opening the inspector.
- [2026-06-21 · Codex] Moved relationship/support copy out of the prototype factor card and onto focused edge labels. Node cards should show the variable itself (name, current metric/trend, `market weight`, evidence/source/credibility); edges should show the relationship (`rolls up`, `raises`, `relieves`, `conditions`, `supports read · s`). Why: influence and hierarchy are already graph semantics, so duplicating them inside every node adds noise and weakens the graph.
- [2026-06-21 · Codex] Wired Run Protocol closer to the real Python core protocol: `server.run_default_tier` now runs `trace_engine.aggregate()` from fixture-backed real inputs and returns `scenario_distribution`, `factor_support`, and server-side `factor_weights`; TraceWorkspace now prioritizes those protocol outputs instead of locally reconditioning outcome percentages. Personal variables remain a separate personal-fit layer and do not mutate market evidence/support. Why: the core protocol should be the source of truth for outcome support and graph weights, while the frontend only renders those results.

- [2026-06-21 · claude] DEPLOYED to https://megathon.traceintelligence.io — live & verified (homepage 200, /api/health + run-personal-advice + factor-tree all 200 over HTTPS). VPS = Hetzner nbg1 (Ubuntu 24.04, 178.104.155.160, root login via Windows ~/.ssh/id_ed25519). Stack: uvicorn under systemd unit `trace-api` (127.0.0.1:8000, enabled/auto-restart) + Caddy auto-TLS serving /srv/megathon/dist and reverse-proxying /api/* via `handle` blocks (NOT try_files matcher — try_files rewrites before reverse_proxy and ate /api). Upload via tar-over-ssh (no rsync on Windows client). Redeploy: `SSH_HOST=root@178.104.155.160 bash deploy/deploy.sh`. Gotcha logged re: local C: was 100% full (WeChat). — single-box same-origin per prior decision.
- [2026-06-21 · Codex] Canvas graph now renders the main factor tree as adjacent hierarchy only: cross-layer causal/feedback/confounder edges remain in the research data but are not drawn on the primary canvas skeleton. Driver nodes route through a visual `Trace Core Protocol` bridge before `market_state`, and hierarchy edges use arrowheads instead of `rolls up` labels; the bridge is visual UI only and receives no Cala/source provenance. Why: the current UI should read as a layered causal framework, with detailed relationship semantics reserved for data/inspector layers rather than cluttering the graph.
- [2026-06-21 · claude · FINAL, supersedes the OutputPanel side-panel approach] The outcome conclusions (the ranked scenario READ) render as ON-CANVAS factor cards — the SAME CanvasGraph card component as the causal/input factors — in a conclusions column to the RIGHT of the measured-price outcome (TraceWorkspace conclNodes at CONC_X=2002, y=838+i·130, group "outcome", wired from price_transaction_outcome; leading read highlighted via CanvasGraph `lead`). The dedicated right-hand OutputPanel side-panel for conclusions has been REMOVED and must not be reintroduced. Rationale (user): conclusions are part of the same reasoning graph and must look identical to the input factor components, not live in a separate panel. The right side keeps only the clean inspector (factor/personal detail + Inputs).
- [2026-06-20 · claude] Added a DEFAULT (home) graph view per mian. defaultView() centers on the graph (factors bbox) and picks k so the node label renders ~14.5px on screen regardless of container (k = 14.5/(13*fitScale), clamp 0.6-1.8) — i.e. font-size-baseline legibility, not fit-to-all; edges overflow, pan to browse. Rewired Codex's exitFocus (the 'Esc · graph' button + Escape key) to call defaultView() instead of fitView() so leaving a focused component returns to the legible centered home (was zooming out to tiny fit-all). Added useEffect([source]) → defaultView() so it settles to the home view once the graph data loads. Codex's focus-on-select (zoom to node) is kept: click→focus node, Esc→default home.
- [2026-06-21 · claude] Force balance (BottomDock) now derives from the engine scenario distribution (dist.w) by each scenario's price direction (SCEN_META.dir: shortage/financing = up, investor = down, regional/user_constraint = flat → excluded), not the hand-set indicator priceStrength. upPct = Σw(up)/(Σw(up)+Σw(down)); the bar segments are scenarios sized by their share of the up/down mass. So the number traces to conditionDistribution output (north-star compliance) and shifts with the conditioned read. The INDICATORS list below remains a static watchlist (separate concern). BottomDock now takes a `dist` prop; removed its hand-rolled INDICATORS-strength force-balance and its ✕ close button (panels toggle via the nav). Also: entering full screen auto-hides both panels; clicking a component re-calls only the inspector (focusComponent), never the indicators dock.
